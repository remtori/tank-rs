[package]
name = "tank-game-server"
version = "0.0.0"
edition = "2018"

[dependencies]
# Error handling
anyhow = "1.0"

# Cli argument parser
clap = "2.0"

# Logging utility
log = "0.4"
flexi_logger = "0.19"

# Websocket Impl
tungstenite = "0.15"

[features]
default = [ "tick-log" ]
tick-log = []
